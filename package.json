{
  "name": "threads-autopilot-web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:create": "wrangler d1 create threads_autopilot",
    "db:migrate:local": "wrangler d1 migrations apply threads_autopilot --local",
    "db:migrate:remote": "wrangler d1 migrations apply threads_autopilot --remote"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260329.0",
    "typescript": "^5.8.3",
    "wrangler": "^4.11.1"
  }
}
